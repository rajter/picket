#pragma once

enum class DisplayServer { x11, Wayland };

enum class MagnifierPosition { LeftUp, RightUp, LeftDown, RightDown };

enum class ColorSpace { RGB, HSL, CMYK };

enum class UiEditType { Scale, Entry };